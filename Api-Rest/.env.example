# ===========================================
# Api-Rest - Variables de Entorno
# ===========================================

# Puerto del servicio
PORT=3000

# ===========================================
# Base de Datos PostgreSQL
# ===========================================
DB_TYPE=postgres
# Para Docker: usar nombre del contenedor (api-postgres-sistema-chifles)
# Sin Docker: usar localhost
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASSWORD=admin123
DB_NAME=sistema-chifles

# ===========================================
# JWT Configuration
# ===========================================
JWT_SECRET=chifles_super_secret_jwt_key_2024

# ===========================================
# WebSocket Server (Go)
# ===========================================
# Para Docker: usar host.docker.internal o IP del host
# Sin Docker: usar localhost
WS_URL=http://localhost:8080/notify
WS_SECRET=super_secret_key_123
