# ===========================================
# Auth-Service - Variables de Entorno
# ===========================================

# Puerto del servicio
PORT=3001
NODE_ENV=development

# ===========================================
# Base de Datos PostgreSQL
# ===========================================
# Para Docker: usar nombre del contenedor (auth-db)
# Sin Docker: usar localhost
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=auth_user
DB_PASSWORD=auth_secret_2024
DB_DATABASE=auth_db

# ===========================================
# JWT Configuration
# ===========================================
JWT_SECRET=chifles_super_secret_jwt_key_2024
JWT_ACCESS_EXPIRES=15m
JWT_REFRESH_EXPIRES=7d
